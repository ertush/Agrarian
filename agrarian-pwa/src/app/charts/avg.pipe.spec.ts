/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AvgPipe } from './avg.pipe';

describe('Pipe: Teste', () => {
  it('create an instance', () => {
    let pipe = new AvgPipe();
    expect(pipe).toBeTruthy();
  });
});
